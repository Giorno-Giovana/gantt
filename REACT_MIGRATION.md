# React Migration Guide

## Первый шаг миграции завершён ✅

Создан минимальный React wrapper, который позволяет использовать Frappe Gantt как React компонент, сохраняя весь существующий функционал.

## Что сделано

### 1. Установлены зависимости
- `react` и `react-dom` - основные библиотеки React
- `@types/react` и `@types/react-dom` - TypeScript типы
- `@vitejs/plugin-react` - плагин Vite для React

### 2. Обновлена конфигурация

**vite.config.ts:**
- Добавлен React плагин для dev режима
- Сохранён library mode для production сборки
- Теперь `npm run dev` запускает React приложение
- А `npm run build` собирает библиотеку как раньше

**tsconfig.json:**
- Добавлена поддержка JSX с `"jsx": "react-jsx"`

### 3. Созданы новые файлы

**src/GanttReact.tsx** - React wrapper компонент:
```tsx
import GanttReact from './GanttReact';

function App() {
  const ganttRef = useRef();

  return (
    <GanttReact
      ref={ganttRef}
      tasks={tasks}
      options={options}
    />
  );
}
```

**src/main.tsx** - точка входа для React приложения с примером использования

**index.html** - новый HTML файл для React (старый сохранён как `index-vanilla.html`)

## Как использовать

### Запуск React приложения

```bash
npm install  # или bun install
npm run dev  # запустит Vite dev server с React
```

Откройте браузер на `http://localhost:5173`

### API React компонента

```tsx
interface GanttReactProps {
  tasks: Task[];
  options?: GanttOptions;
}

// Ref API для императивного управления
interface GanttReactHandle {
  getInstance: () => Gantt | null;
  changeViewMode: (mode: string, maintainPos?: boolean) => void;
  updateOptions: (options: GanttOptions) => void;
  scrollCurrent: () => void;
  updateTask: (id: string, newDetails: Partial<Task>) => void;
  refresh: (tasks: Task[]) => void;
}
```

### Пример использования

```tsx
import { useRef } from 'react';
import GanttReact from './src/GanttReact';

function MyApp() {
  const ganttRef = useRef<GanttReactHandle>(null);

  const tasks = [
    {
      id: '1',
      name: 'Task 1',
      start: '2024-01-01',
      end: '2024-01-05',
      progress: 50
    }
  ];

  const options = {
    view_mode: 'Day',
    on_click: (task) => console.log('Clicked:', task),
    on_date_change: (task, start, end) => {
      console.log('Date changed:', task, start, end);
    }
  };

  return (
    <div>
      <button onClick={() => ganttRef.current?.changeViewMode('Week')}>
        Switch to Week
      </button>
      <GanttReact ref={ganttRef} tasks={tasks} options={options} />
    </div>
  );
}
```

## Архитектура

```
Текущий подход:
┌─────────────────┐
│  GanttReact.tsx │  ← React компонент (обёртка)
│   (wrapper)     │
└────────┬────────┘
         │
         ↓
┌────────────────┐
│  Gantt (class) │  ← Оригинальный класс (без изменений)
│   vanilla TS   │
└────────────────┘
```

### Преимущества этого подхода:

✅ **Минимальные изменения** - весь существующий код работает как раньше
✅ **Обратная совместимость** - можно использовать и vanilla, и React версию
✅ **Постепенная миграция** - можно переписывать компоненты по одному
✅ **Работает сразу** - готовый React компонент с полным функционалом

## Дальнейшие шаги миграции

После этого минимального шага можно постепенно мигрировать внутренние части:

### Шаг 2: Вынести состояние в React
- Использовать `useState` для задач и опций
- Убрать императивные обновления

### Шаг 3: Разбить на компоненты
```tsx
<Gantt>
  <GanttHeader />
  <GanttGrid />
  <GanttBars />
  <GanttArrows />
</Gantt>
```

### Шаг 4: Переписать Bar на React
- Преобразовать `Bar` класс в функциональный компонент
- Использовать React для SVG рендеринга

### Шаг 5: Переписать остальные части
- Arrow → React компонент
- Popup → React компонент
- Grid → React компонент

### Шаг 6: Полная миграция
- Убрать класс Gantt
- Оставить только React компоненты

## Файловая структура

```
gantt/
├── src/
│   ├── index.ts              # Оригинальный класс Gantt
│   ├── GanttReact.tsx        # ✨ НОВЫЙ: React wrapper
│   ├── main.tsx              # ✨ НОВЫЙ: React entry point
│   ├── bar.ts                # Оригинальный код (можно мигрировать позже)
│   ├── arrow.ts              # Оригинальный код (можно мигрировать позже)
│   └── ...                   # Остальные файлы без изменений
├── index.html                # ✨ НОВЫЙ: для React
├── index-vanilla.html        # Старый HTML (сохранён)
├── vite.config.ts            # ✨ ОБНОВЛЁН: React плагин
└── tsconfig.json             # ✨ ОБНОВЛЁН: JSX support
```

## Важные замечания

1. **Сборка библиотеки не изменилась** - `npm run build` продолжает собирать vanilla версию
2. **React используется только в dev режиме** - для разработки и тестирования
3. **Все события работают** - onClick, onDateChange, onProgressChange и т.д.
4. **TypeScript ошибки в оригинальном коде** - не критично, можно исправить постепенно

## Следующий шаг

Выберите один из вариантов:

1. **Продолжить использовать wrapper** - уже готов к использованию
2. **Начать миграцию Bar компонента** - первый внутренний компонент
3. **Вынести состояние в React** - убрать зависимость от класса Gantt
4. **Создать полноценный React компонент** - полная переработка

Wrapper уже работает и готов к использованию! 🎉
