# Конфигурация Bun
[test]
# Загружать test-setup.ts перед запуском тестов
preload = ["./test-setup.ts"]

# Таймаут для тестов (в миллисекундах)
timeout = 5000

# Повторять упавшие тесты
# retries = 2
